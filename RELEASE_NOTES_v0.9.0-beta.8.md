# v0.9.0-beta.8 릴리스 노트

날짜: 2026-08-27

beta.8은 beta.7의 오프닝·캠페인 영상 51편, 57개 장면·388개 한국어 자막과 싱크·깜빡임 교정을 그대로 유지하면서, 글꼴 기준선과 이미지 버튼 생성 방식을 개선한 배포판입니다.

## 렌더러 v3

- 일반 글꼴 13x14 셀·advance 13, 작은 글꼴 11x12 셀·advance 11 계약을 유지합니다.
- 선택 글꼴과 보완 글꼴이 실제로 담당하는 글리프의 공통 ink union으로 가장 큰 정수 픽셀 크기와 한 줄 기준선을 정합니다.
- 각 글리프를 `baseline + glyph.top`에 놓아 FreeType bearing을 보존합니다. 글자마다 확대·축소하거나 셀 바닥에 강제로 맞추지 않습니다.
- 874자 전경 clip은 0이어야 하며 manifest와 설치 receipt가 renderer id, 기준선 정책과 해석 결과를 검증합니다.

## 사용자 글꼴 선택

- 기본 `INSTALL.cmd`는 SIL Open Font License 1.1의 `NanumGothicCoding Regular`를 사용합니다.
- `INSTALL_CUSTOM_FONT.cmd` 또는 `--font-file`·`--font-index`로 사용자가 보유한 로컬 TTF·OTF·TTC·OTC를 선택할 수 있습니다.
- 선택 글꼴에 없는 매핑 문자는 동봉한 나눔고딕코딩으로 보완합니다.
- 선택 파일은 설치 시 로컬에서만 읽고 패치 폴더·게임 폴더·저장소·배포 ZIP에 복사하지 않습니다. 해당 글꼴을 사용할 권한은 사용자가 확인해야 합니다.

개발 중에는 로컬의 `Iropke Batang Medium`을 사용자 선택 예시로 사용해 874자, `주/조/소`의 공통 수직 위치와 전경 clip 0을 분리 후보에서 수동 점검했습니다. 이 글꼴 파일, 라이선스 파일, 그 글꼴로 미리 만든 래스터·AGG는 저장소나 배포 ZIP에 포함하지 않습니다.

## 동적 버튼과 모집 비용 문구

- 게임 글리프와 한글화 대상으로 선언한 버튼 글씨를 같은 선택·보완 글꼴 계획으로 설치 시 생성합니다.
- 모집 창의 `Cost per troop:` 영역은 같은 작은 글꼴로 만든 `병력당 비용:`으로 바꿉니다.
- 메인 메뉴의 원본 배경·영웅 그림·장식·버튼 질감과 선언된 글씨 영역 밖 픽셀은 보존합니다. 한글화 대상으로 선언하지 않은 이미지 UI도 원본을 유지합니다.
- 완성 AGG, 버튼 PNG·래스터나 사용자 선택 글꼴을 배포하지 않습니다. 정확한 GOG 원본과 로컬 글꼴 입력에서 설치기가 결과를 생성합니다.

## 설치와 직접 업그레이드

- 깨끗한 GOG 영문판: beta.8 ZIP의 `INSTALL.cmd` 또는 `INSTALL_CUSTOM_FONT.cmd`
- 공개 beta.4·beta.5·beta.6·beta.7 설치본: 제거하지 않고 beta.8의 두 설치 진입점 중 원하는 방식을 실행
- beta.1~beta.3 설치본: 해당 버전의 `UNINSTALL.cmd`로 GOG 원본을 복구한 뒤 beta.8 설치

beta.8은 공개 beta.4~beta.7 manifest를 원문 그대로 포함하고 크기와 SHA-256을 고정합니다. beta.7 입력은 33,369바이트, SHA-256 `F71C83895BDC3581F1C8BA4BC7919153E14F0500831D941DAE9B34D17519E2CE`입니다. 설치기는 이전 receipt와 현재 설치 파일을 검증한 뒤 최초 GOG 원본 백업에서 beta.8 후보를 만들고, 실패하면 업그레이드 직전 상태로 롤백합니다. beta.8을 제거하면 이전 베타가 아니라 최초 GOG 원본을 복원합니다.

## 범위와 알려진 제한

- 일반 MP2/MX2 사용자 지도 문장은 번역 범위에서 제외합니다.
- 한글화 대상으로 선언하지 않은 그림 제목·퍼즐 화면과 이미지 UI는 원본 영어로 표시됩니다.
- 원본 SMK 영상·음성, 음악, DOSBox, GOG 구성요소와 세이브 파일은 수정하지 않습니다.
- 설치 뒤 `VERIFY.cmd`로 51개 설치 파일과 receipt를 확인할 수 있습니다.
