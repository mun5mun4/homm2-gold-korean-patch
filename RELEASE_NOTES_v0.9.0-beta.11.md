# Heroes II Gold 한국어 패치 v0.9.0-beta.11

릴리스 날짜: 2026-09-07

한글화된 버튼 전체의 도트를 원본 게임 스타일에 맞췄습니다. 갈색·회색·금색 버튼에 맞춰 글씨의 명암과 가장자리를 다시 구성하고, 눌림 상태와 창 배경에 포함된 버튼도 함께 맞췄습니다. 기본 글꼴로 만든 수정본은 실제 게임에서 사용자 확인을 마쳤습니다.

## 주요 변경

- 56개 한글 문구·254개 버튼 상태에 원본 스타일 적용. 원래 평면인 확장 캠페인 이름 5개는 유지
- 원본 256색 팔레트에서 재질과 눌림 상태에 맞는 색조를 사용하고 버튼 크기·위치·테두리 보존
- 창 배경에 포함된 버튼 41개 위치를 같은 결과로 동기화
- 큰 메뉴 버튼, 새 게임 메뉴와 성 선택 버튼에 남아 있던 영문 획 정리
- beta.10의 영상 자막, 이롭게 바탕체·렌더러 v3·사용자 글꼴 선택 기능 유지
- 공개 beta.4~beta.10에서 직접 업그레이드 지원

## 설치

1. Heroes II, DOSBox, GOG Galaxy를 모두 종료합니다.
2. `homm2-ko-v0.9.0-beta.11-win-gog.zip`을 내려받아 모두 풉니다.
3. 기본 이롭게 바탕체는 `INSTALL.cmd`, 사용자가 보유한 글꼴은 `INSTALL_CUSTOM_FONT.cmd`를 실행합니다.

beta.4~beta.10 사용자는 기존 버전을 제거하지 않고 바로 설치할 수 있습니다. beta.1~beta.3 사용자는 먼저 해당 버전의 `UNINSTALL.cmd`로 GOG 원본을 복구해야 합니다. 자세한 설치·복구·제거 방법은 ZIP 안의 `INSTALL_KO.md`를 확인하세요.

지원 대상은 GOG gameId `1207658785`, buildId `52745329670822422`, English DOS판입니다.

## 배포 형식

기존 배포 방식대로 바이너리 델타, 프로젝트 문자열 은행, 설치기 소스와 OFL 1.1 글꼴을 포함합니다. 완성 게임 실행 파일·AGG·캠페인 맵·원본 영상·미리 생성한 버튼 래스터는 포함하지 않으며, 버튼과 글리프는 설치 시 사용자의 지원 원본과 선택 글꼴에서 생성합니다.

## 검증

- 기본 글꼴로 재생성한 두 AGG가 사용자 실기 승인본과 바이트 단위로 일치
- Windows 설치 실행 파일의 새 설치·beta.10 직접 업그레이드·51개 결과 파일 검증 통과
- 원본 GOG와 기본 이롭게 바탕체·사용자 맑은 고딕을 사용한 글꼴 통합 검사 38개 및 새 버튼 회귀 검사 5개 통과
- 업그레이드 중 두 번째 AGG 교체 실패를 주입했을 때 이전 파일과 설치 기록이 정확히 복원됨을 확인

각 다운로드의 해시는 함께 제공하는 `SHA256SUMS.txt`에서 확인할 수 있습니다.
