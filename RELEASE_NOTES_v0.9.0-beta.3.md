# Heroes of Might and Magic II Gold 한국어 패치 v0.9.0-beta.3

GOG 영문 DOS판을 위한 베타 업데이트입니다. 실제 플레이에서 확인된 외부
문자열 은행의 실행 주소 문제를 바로잡아, 중립 부대 합류 문장을 포함한 H2K3
문자열이 안정적으로 적용되도록 했습니다.

## 업데이트 설치

1. GOG Galaxy, Heroes II와 DOSBox를 종료합니다.
2. beta.2를 설치했다면 beta.2 배포 폴더의 `UNINSTALL.cmd`를 먼저 실행합니다.
3. `homm2-ko-v0.9.0-beta.3-win-gog.zip`을 모두 풉니다.
4. 새 폴더의 `INSTALL.cmd`를 실행합니다.

설치기는 이미 수정된 파일에 델타를 겹쳐 적용하지 않습니다. beta.2 배포
폴더가 없다면 GitHub Releases에서 beta.2 ZIP을 다시 받아 `UNINSTALL.cmd`를
실행하세요.

기본 설치 경로는 자동으로 확인합니다.

```text
C:\Program Files (x86)\GOG Galaxy\Games\HoMM 2 Gold
```

지원 원본은 GOG gameId `1207658785`, buildId `52745329670822422`, English
설치본입니다. 원본 50개를 전수 확인하며 다른 빌드나 수정본은 변경 전에
거부합니다.

## beta.3 변경 사항

- H2K3 외부 문자열 은행을 첫 문자열 출력 시점이 아니라 게임 본체 진입 전에 초기화
- Object2 선호 주소를 사용하던 문장 descriptor 155개와 로더 범위를 실기 재배치 주소 `+0x204000`으로 교정
- 중립 부대 합류 제목과 동적 문장을 한국어로 표시하고 EXE 예비 문구도 함께 교정
- 대상 주문 메시지의 재배치 누락을 수정해 `영웅: 대상에게 주문 시전.` 어순을 안정화
- 기존 캠페인·전투 로그·`공기정령` 폴백 교정을 모두 유지

## 포함 범위

- 오리지널·확장 캠페인 47개 번역
- 주요 비이미지 UI, 전투·주문·아티팩트·모험 시설 문구
- H2K3 외부 문자열 은행과 874자 한글 매핑
- 캠페인 미션 전환 정지 및 전투 로그 후속 교정

## 배포 방식과 알려진 제한

원작 완성 파일은 포함하지 않습니다. ZIP에는 정품 GOG 원본에만 적용되는
BSDIFF40 델타 50개와 프로젝트 파일 `KOREAN.BIN`이 들어 있습니다.

- 그림에 박힌 영문 버튼·제목은 아직 남아 있습니다.
- 일반 MP2/MX2 지도는 번역하지 않습니다.
- 장기 캠페인 문맥과 회귀 검증은 베타 피드백으로 이어갑니다.
- 코드 서명이 없어 Windows SmartScreen 경고가 표시될 수 있습니다.
