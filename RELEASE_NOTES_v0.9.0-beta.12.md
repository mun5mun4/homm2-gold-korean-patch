# Heroes II Gold 한국어 패치 v0.9.0-beta.12

릴리스 날짜: 2026-09-07

큰 메뉴와 모집 창의 `최대` 버튼에서 글씨가 아래로 치우쳐 보이던 문제를 고쳤습니다. beta.11의 글씨 모양과 입체 표현을 유지하면서 실제 버튼 평면의 중앙으로 옮기고 아래쪽 테두리를 복원합니다.

## 주요 변경

- 132×62 큰 메뉴 버튼 89개 상태: 밝은 왼쪽 아래 가장자리를 포함한 글씨를 중앙에 맞춰 기본 글꼴 결과에서 3픽셀 위로 이동
- 모집 창 `최대` 버튼: 일반 상태를 3픽셀, 눌린 상태를 4픽셀 올리고 기존 글씨·그림자가 덮었던 아래쪽 테두리를 원본으로 복원
- 총 91개 상태·16개 리소스(기본 13개·확장 3개) 교정; 나머지 163개 버튼 상태와 글리프·색조 유지
- beta.11의 입체 스타일, beta.10의 EXE·영상 자막·문자열 은행과 렌더러 v3·사용자 글꼴 선택 유지
- 공개 beta.4~beta.11에서 직접 업그레이드 지원

beta.12의 중앙 배치·보존 범위는 소스 회귀검사와 격리된 fixture에서 확인했습니다.

## 설치

1. Heroes II, DOSBox, GOG Galaxy를 모두 종료합니다.
2. `homm2-ko-v0.9.0-beta.12-win-gog.zip`을 내려받아 모두 풉니다.
3. 기본 이롭게 바탕체는 `INSTALL.cmd`, 사용자가 보유한 글꼴은 `INSTALL_CUSTOM_FONT.cmd`를 실행합니다.

beta.4~beta.11 사용자는 기존 버전을 제거하지 않고 바로 설치할 수 있습니다. beta.1~beta.3 사용자는 해당 버전의 `UNINSTALL.cmd`로 GOG 원본을 복구한 뒤 설치합니다. 자세한 설치·복구·제거 방법은 ZIP 안의 `INSTALL_KO.md`를 확인하세요.

지원 대상은 GOG gameId `1207658785`, buildId `52745329670822422`, English DOS판입니다.

## 배포 형식

바이너리 델타, 프로젝트 문자열 은행, 설치기 소스와 OFL 1.1 글꼴을 포함합니다. 완성 게임 실행 파일·AGG·캠페인 맵·원본 영상·캡처·미리 생성한 버튼 래스터는 포함하지 않습니다. 글리프와 중앙 배치한 버튼은 설치 시 지원 원본과 선택 글꼴에서 생성합니다.

각 다운로드의 해시는 함께 제공하는 `SHA256SUMS.txt`에서 확인할 수 있습니다.
